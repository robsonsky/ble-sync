package com.hailie.demo.di

import org.koin.dsl.module

val portsModule =
    module {
        // Only interfaces here; implementations are bound in other modules
        // This module is optional; included for clarity of intent.
    }
