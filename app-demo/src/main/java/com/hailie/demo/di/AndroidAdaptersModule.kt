package com.hailie.demo.di

import org.koin.dsl.module

// TO DO: bind real Android adapters here when ready
val androidAdaptersModule =
    module {
        // e.g. single<BlePort> { AndroidBlePortImpl(androidContext()) }
    }
