package com.hailie.ports

interface BlePort {
    // suspend fun connect(deviceId: String): Result<Unit>
    // suspend fun read(characteristic: String, mtu: Int? = null): Result<ByteArray>
    // suspend fun write(characteristic: String, payload: ByteArray, withResponse: Boolean = true): Result<Unit>
    // suspend fun disconnect(): Result<Unit>
}
