package com.hailie.ports

interface TelemetryPort {
    // fun log(event: String, attrs: Map<String, Any?> = emptyMap())
    // fun metric(name: String, value: Number, tags: Map<String, String> = emptyMap())
}
